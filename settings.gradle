plugins {
    // Lets the Java toolchain auto-download JDK 21 even when only JDK 17 is installed locally.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '0.9.0'
}

rootProject.name = 'basin-evacuation-decision'
